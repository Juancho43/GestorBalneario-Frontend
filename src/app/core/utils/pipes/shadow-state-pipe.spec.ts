import { ShadowStatePipe } from './shadow-state-pipe';

describe('ShadowStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ShadowStatePipe();
    expect(pipe).toBeTruthy();
  });
});
